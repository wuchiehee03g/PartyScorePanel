# BetPanel 低摩擦房間復原設計

目標是讓一次性付費主持人不必註冊傳統帳號，也能在重新整理、關閉頁面或換裝置後安全找回房間。玩家維持匿名，不需要任何復原功能。

## 主持人看到的流程

1. 主持人完成 NT$200 付款。
2. 後端確認支付商 webhook 後立即建立房間，建立當下開始固定六小時。
3. 同一個瀏覽器自動保留主持人登入狀態，重新整理或重開會回到原房間，期限不重算。
4. 開房完成頁同時顯示兩個不同的 QR Code：
   - **活動 QR Code**：公開給玩家，只能加入活動。
   - **私人管理 QR Code**：只給主持人，可截圖保存，用來在新裝置復原主持人身分。
5. 主持人可選填 Email。若有填寫，後端寄送同一用途的私人管理連結；不填也能使用管理 QR Code。
6. 新裝置開啟私人管理連結後，後端驗證一次性權杖並恢復原本的 Firebase 主持人 UID，再顯示原房間與原剩餘時間。

這個流程沒有帳號名稱、密碼、會員中心或玩家註冊。Email 只是選填的復原備援，不是加入活動的必要條件。

## 活動代碼不能用來接管後台

六位活動代碼會分享給所有玩家，因此只能用來讀取公開活動及提交自己的預測。下列資訊都不能單獨取得主持人權限：

- 活動代碼
- 活動 QR Code
- 主持人暱稱
- 房間 PIN
- 公開積分報告

目前的 `config.pin` 只保留既有本機辨識相容，不是正式安全憑證。

## 私人復原權杖

- 由可信後端產生至少 128-bit 的不可預測隨機權杖。
- 原始權杖只出現在管理連結／QR Code，伺服器只保存加鹽雜湊。
- 權杖不寫入 `rooms/{roomId}`，也不出現在任何玩家可讀資料。
- 預設只能成功兌換一次；完成新裝置復原後立即失效並核發新的備援權杖。
- 可由客服或原主持人撤銷、輪替；所有成功及失敗嘗試保留稽核紀錄。
- 房間到期後不能藉由權杖延長 `expiresAt`。若仍允許復原，只能查閱及完成既有善後操作。
- 復原端點需要速率限制、失敗次數限制及異常告警，不能用六位活動代碼取代長權杖。

## 不改既有 hostUid 的復原方式

後端驗證復原權杖後，使用 Firebase Admin SDK 為原本的 `hostUid` 建立短效 Custom Token。新裝置以 Custom Token 登入後仍取得原 UID，因此：

- 不需要修改 Firebase 內既有的 `hostUid`、`hostProfile`、`hostName` 命名。
- 不需要把房間所有權轉移給新的匿名 UID。
- 現有「只有該 hostUid 可以管理房間」的 Database Rules 可以繼續使用。

Custom Token 只能由可信後端核發，不能把 Admin SDK、服務帳號或簽章密鑰放在 GitHub Pages。

## Email 選填規則

- Email 應存放在私有訂單／復原資料庫，不能放入公開房間 config。
- 收集前清楚標示用途：「寄送房間管理連結與處理付費訂單問題」。
- 不用於行銷，除非使用者另行主動同意。
- 管理郵件不直接顯示完整復原權杖文字，只提供 HTTPS 管理按鈕及基本訂單資訊。
- 需定義保存期限、退訂／更正／刪除管道與寄信失敗處理方式。

## 建議的私有資料

以下只是後端資料契約，不可讓 GitHub Pages 直接寫入：

```text
privateOrders/{orderId}
  roomId
  hostUid
  recoveryEmail?       # 選填
  paymentStatus
  paidAt
  createdAt

roomRecovery/{recoveryId}
  roomId
  hostUid
  tokenHash
  status               # active / consumed / revoked / expired
  createdAt
  consumedAt?
  revokedAt?
```

上述節點應由 Cloud Functions／可信伺服器使用 Admin SDK 管理，前端 Database Rules 一律不提供直接讀寫。

## 正式驗收情境

- 同瀏覽器重新整理、關閉後重開：自動回到原房間，剩餘時間持續減少。
- 清除網站資料或換裝置：活動代碼不能接管；私人管理 QR 可以復原。
- Email 未填：管理 QR 仍可復原。
- Email 已填：收到管理連結；信件遺失不影響原瀏覽器使用。
- 權杖被使用第二次、猜測、撤銷或過期：必須拒絕。
- 復原成功：UID 與原 `hostUid` 相同，房間資料及期限不變。
- 房間已封存：復原後只能查閱，不能重新啟用。
- 房間已到期：不能新增題目、預測或戰況，也不能延長六小時。
