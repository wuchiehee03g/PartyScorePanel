# BetPanel 好友免費場次設計

這是軟體服務優惠，不是活動積分兌換。好友體驗碼只能免除一次 NT$200 的房間使用費，不能換成現金、活動 Pts、獎品、退款或其他利益。

## 使用者流程

1. 使用者在正式購買頁選擇「使用好友體驗碼」。
2. 輸入體驗碼與 Email。
3. 後端寄送一次性 Email 驗證連結。
4. 使用者點擊後，後端以交易檢查：
   - 活動碼存在、啟用中且未過期。
   - Email 已驗證。
   - 同一 Email 尚未領取過這組活動碼。
   - 活動碼尚未超過總兌換上限。
5. 後端立即建立一個六小時房間，建立當下開始倒數。
6. 同一封信同時提供私人管理連結，作為跨裝置復原備援。

一般付費開房的 Email 仍可選填；只有領取免費優惠時需要驗證 Email，因為完全匿名無法可靠阻止清除網站資料後重複領取。

## 推薦的活動型態

- 一組人類可輸入但不可輕易猜中的好友體驗碼。
- 同一個已驗證 Email 對該活動碼限領一次。
- 設定活動開始／結束時間及總兌換上限，代碼外流時可立即停用。
- 初期朋友推廣建議設定小額總名額，再依實際使用狀況增加。
- 不建立上線／下線、返佣、分潤或多層推薦關係。

如果完全不想驗證 Email，替代方案是為每位朋友產生不同的一次性邀請碼；不能用同一組可重複使用的碼可靠判斷「每人一次」。

## 後端資料模型

```text
promoCampaigns/{campaignId}
  codeHash
  status                 # active / paused / expired
  benefitPlan            # single_room_6h_twd_200_free
  startsAt
  expiresAt
  maxRedemptions
  redemptionCount
  createdAt

promoRedemptions/{campaignId}/{emailHash}
  roomId
  hostUid
  redeemedAt
  orderId
```

- 原始體驗碼不寫入 GitHub、不放在前端 JavaScript，也不以明文存入資料庫。
- Email 使用伺服器端正規化後的雜湊作唯一鍵；原始 Email 只存於私有訂單／寄信系統並依隱私政策保存。
- 「檢查尚未兌換、增加計數、建立零元訂單、建立房間」必須在可信後端以交易／冪等流程完成。
- 前端不得直接把房間標成免費，也不得修改 `roomAccess`、優惠活動或兌換紀錄。
- 免費房間仍使用既有六小時、封存、結果不可變與審計規則。

## 與舊 redeemCodes 的關係

這不是舊版點數兌換碼。既有 `/redeemCodes` 繼續保持前端不可讀寫；正式好友體驗碼使用新的私有後端活動與兌換資料，不會建立玩家錢包或增加活動積分。

## 上線前測試

- 正確代碼＋新 Email：只建立一個房間。
- 同 Email 再次兌換：拒絕且不建立房間。
- 大小寫、前後空白或 Gmail 別名等 Email 正規化規則一致。
- webhook／Email 連結重送：不重複建立房間。
- 錯誤、停用、過期或超過名額的代碼：拒絕。
- 同時兩次請求：只有一筆成功。
- 兌換成功但瀏覽器關閉：可由 Email 私人管理連結復原。
- 免費房間重新整理或重開：期限接續，不重新計算六小時。
