# BetPanel 正式收款前的時間流程

這份文件把「付款」與「6 小時活動」分開處理。活動內的 Pts 只是計分；NT$200 只購買一個活動房間的使用時間。

## 建議的正式流程

1. 主辦方建立 NT$200 訂單。
2. 藍新付款成功後，後端收到並驗證 webhook。
3. 付款確認後由後端建立活動房間，建立當下固定記錄開始時間與到期時間。
4. 從房間建立時間起算 6 小時；關閉、重新整理或重新開啟頁面都只會接續原本的剩餘時間，不會重新起算或延長。
5. 到期後停止新題目、新預測與新戰況。
6. 到期後仍可截止既有題目、公布結果、查看報告與封存，避免留下未完成紀錄。

不要在使用者打開結帳頁、跳回付款成功頁或瀏覽器顯示成功時直接起算。真正的付款結果與開始時間都必須由後端確認。

主持人不需要傳統帳號密碼。正式版本採同瀏覽器自動恢復，另提供可截圖保存的私人管理 QR／復原碼；Email 為選填備援，只用來寄送管理連結。活動代碼是公開資訊，不能用來接管後台。完整安全規格見 [ROOM_RECOVERY_DESIGN.md](ROOM_RECOVERY_DESIGN.md)。

## 建議狀態

```text
訂單建立 → 已付款並建立房間 → 活動進行中 → 已到期 → 唯讀保存
                ↘ 退款／拒付／人工處理
```

- `已付款並建立房間`：支付商 webhook 已驗證，金額與訂單一致；後端建立房間並固定 `expiresAt = startsAt + 6 小時`。
- `活動進行中`：同一房間的 `startsAt` 與 `expiresAt` 不可由前端修改，重新進入不會延長。
- `已到期`：不再接受新活動資料，但保留善後與查閱能力。

## 時間上最容易出問題的地方

### 正式收款前一定要解決

1. **換裝置或清除瀏覽器資料**：目前使用 Firebase 匿名登入。UID 遺失後，已付款主辦方可能無法找回房間。正式收款前由可信後端驗證私人復原權杖，並為原 `hostUid` 核發短效 Firebase Custom Token；不能只靠公開活動代碼或 PIN 接管。
2. **付款成功但頁面關閉**：後端必須保存付款與使用權，主辦方重新登入後仍能找到，不能依賴付款完成頁。
3. **webhook 重送**：同一筆付款可能通知多次。後端要做冪等處理，不能重複核發、重複延長或建立多個房間。
4. **付款成功但核發失敗**：需要自動重試、對帳及人工補單紀錄，不能讓使用者付了錢卻沒有使用權。
5. **退款或拒付**：要先定義尚未啟用、已啟用及活動進行中的處理方式，所有異動保留稽核紀錄。

### 現場使用時要處理

1. **剛好在到期邊界送出**：畫面可能仍顯示開放，但送到伺服器時已到期。以 Firebase 伺服器時間與 Rules 為準，前端應明確顯示「可能剛好到期，請重新整理」。
2. **手機切到背景**：瀏覽器會暫停倒數計時。回到頁面時必須重新以伺服器時間計算，不能從舊畫面接著倒數。
3. **斷線後恢復**：只有伺服器確認成功的提交才算有效；離線佇列在到期後送達時應被 Rules 拒絕。
4. **活動到期但尚未公布結果**：禁止新增風險，但主持人必須仍能截止、公布、產生報告與封存。
5. **多分頁或多裝置操作**：所有狀態轉換由 Rules 保護；結果公布後不可撤回，封存後不可再操作。
6. **快取舊版本**：部署時同步更新 `app.js` 與 `style.css` 的 query version，避免現場載入舊邏輯。

## 目前已具備

- 前端倒數使用 Firebase `serverTimeOffset` 校正時間。
- 頁面從背景恢復或重新取得焦點時會立即刷新狀態。
- 6 小時到期的真正寫入邊界由 Realtime Database Rules 的 `now` 判定。
- 到期後停止新預測、新題目與新戰況；仍可完成截止、公布、報告與封存。
- 公布結果不可更改；封存保留題目、預測及審計紀錄。

## 尚未具備，不能在串金流時省略

- 藍新訂單建立、webhook 簽章驗證及後端訂單資料庫。
- 付款確認後一次性建立房間並立即固定六小時期限的後端操作。
- 私人管理 QR／復原權杖、選填 Email 寄信及跨裝置 Custom Token 復原。
- webhook 冪等、失敗重試、對帳、退款與拒付流程。
- 真實客服資料、訂單查詢與退款申請入口。

完成以上項目並取得支付商書面可承作確認後，才適合把 Demo 的「測試啟用」換成正式付款與啟用流程。

送審後的網站維護及重大服務變更界線見 [NEWEB_CONTENT_CHANGE_POLICY.md](NEWEB_CONTENT_CHANGE_POLICY.md)。不要在送審時暫時隱藏敏感範本、核准後再恢復；如要保留妞妞、骰寶等官方預設，應在送審前如實揭露並取得書面確認。
